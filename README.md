# (UNFINISHED) General Server

A reusable niche server interface. Servers are nothing but Remote Code. Public Functions are routes, static functions are middleware, services reference API's, databases, ETC.

# How to use


import <this module>

